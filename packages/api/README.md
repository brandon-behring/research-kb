# Research KB API

FastAPI REST API for the research knowledge base.

## Quick Start

```bash
# Start the API server
uvicorn research_kb_api.main:app --reload

# Or with Docker
docker-compose up api
```

## Endpoints

_No routes found. Run extraction to populate._

## Configuration

Environment variables:

| Variable | Default | Description |
|----------|---------|-------------|
| `DATABASE_URL` | `postgresql://...` | Database connection string |
| `EMBEDDING_SERVER_URL` | `http://localhost:8080` | Embedding server URL |

---
*Generated by scripts/generate_package_docs.py on 2025-12-16*