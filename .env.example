# research-kb environment configuration
# Copy to .env and update values for your setup.

# -- PostgreSQL --
DATABASE_URL=postgresql://research_kb:research_kb@localhost:5432/research_kb

# -- Embedding Server --
EMBED_SOCKET_PATH=/tmp/research_kb_embed.sock

# -- Ollama (local LLM for concept extraction) --
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama3.1:8b

# -- Anthropic (cloud LLM for concept extraction) --
# ANTHROPIC_API_KEY=sk-ant-...

# -- Semantic Scholar (paper discovery) --
# S2_API_KEY=...

# -- Daemon --
# DAEMON_SOCKET_PATH=/tmp/research_kb_daemon.sock

# -- GROBID (PDF extraction) --
GROBID_URL=http://localhost:8070

# -- KuzuDB (graph engine) --
# KUZU_DB_PATH=~/.research_kb/kuzu/research_kb.kuzu
