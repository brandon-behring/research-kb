# One-dimensional MCMC to HMC transition

Start with importance sampling and random-walk Metropolis on a toy target, then map the same model to PyMC or Stan.

## Deliverable

- A short note or notebook that states the modeling goal, the data source, the candidate models, and the evaluation rule.

## Completion criteria

- Satisfies the module checkpoint: You can run and diagnose MCMC/HMC in PyMC and compare one computational workflow to Stan/CmdStanPy.
- Cites at least one primary source and one exercise/data source from this module.
