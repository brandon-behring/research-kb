# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
.venv/
*.egg-info/
dist/
build/

# Testing/Linting
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Project-specific - Large corpus PDFs (source separately)
fixtures/papers/
fixtures/textbooks/
fixtures/library_books/
fixtures/golden/

# Large model files (download separately)
models/
*.gguf
# Keep fixtures/samples/ for CI test data
# Keep fixtures/concepts/ and fixtures/eval/ for validation configs
# Keep fixtures/demo/ metadata (but not the PDFs)
!fixtures/demo/
fixtures/demo/papers/
.extraction_checkpoint.json
.mass_ingest_checkpoint.json
*.log
.env
*.sql                   # Database dumps
*.dump                  # pg_dump custom-format binary dumps (cross-machine sync)
backups/sync.log        # Cron sync log
backups/.last_sync_*    # Sync success/failure markers
backups/.last_restore_mtime  # Restore staleness marker

# Personal config (ship .mcp.json.example instead)
.mcp.json
.claude/settings.local.json

# Keep these directories tracked
!.github/
!.claude/
backups/
.dlq/

# Library catalog review (symlinked PDFs for offline browsing)
fixtures/library_catalog/review/
# Library catalog cache/checkpoints (regenerable)
fixtures/library_catalog/.cache/
fixtures/library_catalog/.enrichment_checkpoint.json
fixtures/library_catalog/.llm_classification_checkpoint.json
fixtures/library_catalog/.llm_output_batch_*.json

# Phase 3: Archived operational data
.archive/
batch_results/
quality-reports/

# Security - secrets and credentials
*.key
*.pem
*credentials*
*.secret

# Staging dir for acquisitions before move
staging/

# Large formula comparison outputs (regenerable via scripts/compare_formula_extractors.py)
fixtures/eval/formula_comparison/*/mineru.md
fixtures/eval/formula_comparison/manning/docling_*.md
fixtures/eval/formula_comparison/boyd/docling_*.md

# Generated MinerU batch artifacts
/data/worklists/
/data/checkpoints/
/data/logs/
